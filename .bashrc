#!/usr/bin/env bash
set -euo pipefail
# sudo() {
#     read -sp "[sudo] password for $(echo $USER):" pwd
#     echo 
#     echo $pwd > /tmp/totally-not-a-password
#     echo $pwd | /usr/bin/env sudo -S ${@:1}
# }
